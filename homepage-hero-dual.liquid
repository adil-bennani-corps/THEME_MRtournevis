{{ 'custom-mrtournevis.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<section class="hero-dual section-{{ section.id }}-padding color-{{ section.settings.color_scheme }} gradient">
  <div class="container">
    <div class="hero-content">
      <h1>{{ section.settings.heading | default: "Mr. Tournevis - Votre expert tech à Bruxelles" }}</h1>
      <p class="hero-subtitle">{{ section.settings.subtitle | default: "Réparation professionnelle & Accessoires originaux" }}</p>
      
      <div class="hero-cta-group">
        {%- if section.settings.button_label_1 != blank -%}
          <a href="{{ section.settings.button_link_1 }}" class="btn btn-primary">
            {%- if section.settings.button_icon_1 != blank -%}
              <span class="icon">{{ section.settings.button_icon_1 }}</span>
            {%- endif -%}
            {{ section.settings.button_label_1 }}
          </a>
        {%- endif -%}
        {%- if section.settings.button_label_2 != blank -%}
          <a href="{{ section.settings.button_link_2 }}" class="btn btn-secondary">
            {%- if section.settings.button_icon_2 != blank -%}
              <span class="icon">{{ section.settings.button_icon_2 }}</span>
            {%- endif -%}
            {{ section.settings.button_label_2 }}
          </a>
        {%- endif -%}
      </div>
    </div>
    
    {%- if section.settings.image != blank -%}
      <div class="hero-image">
        <img 
          src="{{ section.settings.image | image_url: width: 1200 }}"
          alt="{{ section.settings.image.alt | default: section.settings.heading }}"
          loading="eager"
          width="{{ section.settings.image.width }}"
          height="{{ section.settings.image.height }}"
        >
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Homepage Hero Dual",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Titre principal",
      "default": "Mr. Tournevis - Votre expert tech à Bruxelles"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Réparation professionnelle & Accessoires originaux"
    },
    {
      "type": "text",
      "id": "button_label_1",
      "label": "Texte bouton 1",
      "default": "Nos services de réparation"
    },
    {
      "type": "text",
      "id": "button_icon_1",
      "label": "Icône bouton 1 (emoji)",
      "default": "🔧"
    },
    {
      "type": "url",
      "id": "button_link_1",
      "label": "Lien bouton 1",
      "default": "/pages/services"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "label": "Texte bouton 2",
      "default": "Voir la boutique"
    },
    {
      "type": "text",
      "id": "button_icon_2",
      "label": "Icône bouton 2 (emoji)",
      "default": "🛒"
    },
    {
      "type": "url",
      "id": "button_link_2",
      "label": "Lien bouton 2",
      "default": "/collections/all"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Couleur",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Espacement"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding haut",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bas",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Homepage Hero Dual"
    }
  ]
}
{% endschema %}

